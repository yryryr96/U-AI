import ThemeCanvas from "@/commonComponents/themecanvas";

const Fire = () => {
  return (
    <>
      <ThemeCanvas />
      {/* 테마페이지 */}
      {/* 1. 여기서 랜더링 될 컴포넌트를 구분해서 불러온다. */}
      {/* 2. 그냥 컴포넌트 불러온다.*/}
      {/* 2-1. 함수로 구분해서 return 해준다. */}
      {/* 2-2. 스테이트값을 변환시켜서 return */}
      {/* 2-3. 배열을 사용해서 순서대로 return */}
    </>
  );
};

export default Fire;
